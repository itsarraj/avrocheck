//! `avrocheck` — checks two Avro schema versions for backward/forward
//! compatibility against Avro's own documented schema resolution rules.
//! See the README for exactly which type shapes are modeled.

pub mod compat;
pub mod schema;
